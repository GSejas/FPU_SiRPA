Both behavioral and post-synthesis sims hold the input operand vectors for the corresponding testbenches in different precisions.

One needs only run the filelist4sim.sh to generate a file_list file that holds the dirs of all the sources.

get_saif.sh is a script that helps calculate the conmutation inside the sim.

run_sim.sh actually runs the simulations.
